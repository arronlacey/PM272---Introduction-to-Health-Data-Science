# The humvar dataset from above, annotated with VEP 
# and processed to include some features from VEP that might be helpful in predicting disease
humvar<-read.csv("humvar.csv") 

# Summarize values in each column